CREATE TABLE author (
  authorid int(11) NOT NULL default '0',
  aname varchar(15) default NULL,
  acity varchar(15) default NULL,
  acountry varchar(15) default NULL,
  PRIMARY KEY  (authorid)
);

INSERT INTO author VALUES (1000,'nandagopalan','bangalore','india'),
(2000,'tony','haywook','usa'),
(3000,'holzner','new yourk','usa'),
(4000,'tennenbaum','london','uk'),
(5000,'balaguruswamy','chennai','india');

CREATE TABLE catalog (
  bookid int(11) NOT NULL default '0',
  title varchar(20) default NULL,
  authorid int(11) default NULL,
  publisherid int(11) default NULL,
  categoryid int(11) default NULL,
  year int(11) default NULL,
  price int(11) default NULL,
  PRIMARY KEY  (bookid),
  KEY authorid (authorid),
  KEY publisherid (publisherid),
  KEY categoryid (categoryid),
  CONSTRAINT catalog_ibfk_1 FOREIGN KEY (authorid) REFERENCES author (authorid),
  CONSTRAINT catalog_ibfk_2 FOREIGN KEY (publisherid) REFERENCES publisher (publisherid),
  CONSTRAINT catalog_ibfk_3 FOREIGN KEY (categoryid) REFERENCES category (categoryid)
);

INSERT INTO catalog VALUES (123,'dsc',1000,33,2,2000,185);

CREATE TABLE category (
  categoryid int(11) NOT NULL default '0',
  descripition varchar(20) default NULL,
  PRIMARY KEY  (categoryid)
);

INSERT INTO category VALUES (1,'os');

CREATE TABLE order_details (
  orderno int(11) NOT NULL default '0',
  bookid int(11) NOT NULL default '0',
  quantity int(11) default NULL,
  PRIMARY KEY  (orderno,bookid),
  KEY bookid (bookid),
  CONSTRAINT order_details_ibfk_1 
FOREIGN KEY (bookid) REFERENCES catalog (bookid)
);

INSERT INTO order_details VALUES (112,123,100);

CREATE TABLE publisher (
  publisherid int(11) NOT NULL default '0',
  pname varchar(15) default NULL,
  pcity varchar(15) default NULL,
  pcountry varchar(15) default NULL,
  PRIMARY KEY  (publisherid)
);

INSERT INTO publisher VALUES (11,'wiely','new delhi','india');